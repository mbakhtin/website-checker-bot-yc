{
  "name": "templates\\nodejs",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p .",
    "test": "tsc -p . && node dist/local-test.js",
    "deploy": "npm run build && npx --no-install serverless deploy"
  },
  "devDependencies": {
    "@yandex-cloud/function-types": "^2.1.1",
    "@yandex-cloud/serverless-plugin": "^1.7.18",
    "serverless": "^3.38.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@yandex-cloud/nodejs-sdk": "^2.7.4",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.4.5",
    "node-fetch": "^2.7.0",
    "ydb-sdk": "^5.3.2"
  }
}
